
// Setup module
// ------------------------------

document.addEventListener("DOMContentLoaded", function () {
    var controller = "setting/serverside";
    var column = 5;
    datatable(controller, column);
    /* Setting.init(); */
});
